name: Deploy to GitHub Pages

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - run: git config user.name "bengaudry"
      - run: git config user.email "bengaudry@outlook.fr"
      - run: pnpm install
      - run: pnpm run build
      - run: pnpm run deploy
